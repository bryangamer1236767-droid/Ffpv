[phases.setup]
aptPkgs = ["python3", "python3-pip"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn freefire_server_full:app --bind 0.0.0.0:${PORT:-5000} --workers 2 --timeout 120 --access-logfile - --error-logfile -"
