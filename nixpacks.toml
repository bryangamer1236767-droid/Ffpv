[phases.setup]
aptPkgs = ["python3", "python3-pip"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn aisaka_server:app --workers 1 --worker-class gthread --threads 8 --timeout 120 --bind 0.0.0.0:${PORT:-5000} --access-logfile - --error-logfile -"
